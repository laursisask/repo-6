{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "django:server",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": [
                "runserver",
            ],
            "justMyCode": true,
            "env": {
                "DOPPLER_ENV": "1"
            }
        }
    ]
}